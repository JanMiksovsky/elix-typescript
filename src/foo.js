/// <reference path="../types.ts"/>


/**
 * Convert a string to a number.
 * 
 * @type {StringToNumber}
 */
export const string2number =
  /**
   * @param {string} s - The string to parse
   */
  function(s) {
    return parseInt(s);
  };

